[project]
name = "dcp-client"
version = "0.1.0"
description = "Desktop Context Protocol — Python client SDK"
requires-python = ">=3.11"
license = { text = "MIT OR Apache-2.0" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
